import 'package:flutter/material.dart';

class HistoryApprovalScreen extends StatelessWidget {
  const HistoryApprovalScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('History Approval Screen'));
  }
}
